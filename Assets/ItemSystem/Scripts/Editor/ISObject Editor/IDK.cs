using UnityEngine;
using System.Collections;

public class IDK : MonoBehaviour {

  /*  const int SPRITE_BUTTON_SIZE = 46;

    const string DATABASE_NAME = @"ObjectDatabase.asset";
    const string DATABASE_FOLDER_NAME = @"Database";
    const string DATABASE_FULL_PATH = @"Assets/" + DATABASE_FOLDER_NAME + "/" + DATABASE_NAME;


    [MenuItem("Item System/Database/Object Editor %#o")]
    public static void Init()
    {
        ISObjectDatabaseEditor window = EditorWindow.GetWindow<ISObjectDatabaseEditor>();
        window.minSize = new Vector2(400, 300);
        window.titleContent.text = "Object Database";
        window.Show();
    }


    void OnEnable()
    {
        objectDatabase = ScriptableObject.CreateInstance<ISObjectDatabase>();
        objectDatabase = objectDatabase.GetDatabase<ISObjectDatabase>(DATABASE_FOLDER_NAME, DATABASE_NAME);
    }

    void OnGUI()
    {
        //  ListView();

        GUILayout.BeginHorizontal("Box", GUILayout.ExpandWidth(true));
        BottomBar();
        GUILayout.EndHorizontal();
    }

    void BottomBar()
    {
        GUILayout.Label("Item Count: " + objectDatabase.Count);

        if (GUILayout.Button("Add"))
        {
            objectDatabase.Add(new ISObject());
        }
    } */
}
